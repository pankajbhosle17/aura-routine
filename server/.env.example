# Copy this file to .env and adjust as needed.

# Port the Express API listens on.
PORT=4000

# Path to the compiled C++ engine binary (relative to server/, or absolute).
# Build it first with: cd cpp-engine && make
ENGINE_PATH=../cpp-engine/engine

# Where the SQLite database file should live.
DB_PATH=./data/aura-routine.db
